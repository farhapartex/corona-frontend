import { API_HOST } from "@/global";
const API_URL: string = `${API_HOST}/api/v1`;

export const BD_INFO_ENDPOINT: string = `${API_URL}/bd-infos/`;
export const GLOBAL_INFO_ENDPOINT: string = `${API_URL}/global-infos//`;